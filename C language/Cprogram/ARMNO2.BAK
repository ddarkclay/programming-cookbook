#include<stdio.h>
#include<conio.h>
void main()
{
int no,temp,arm=0;
scanf("%d",&no);
for(no = 1;no<=500;no++)
{
   while(no!=0)
   {
   temp = no%10;
   arm = arm+(temp*temp*temp);
   no=no/10;
   }
   if(no == arm)
   {
   printf("%d",arm);
   }
}
getch();
}
