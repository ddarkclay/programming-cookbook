#include<stdio.h>
#include<conio.h>
#include<string.h>

void main()
{
 char str[20];
 char newarr[10];
 int i,j=0,no;
 clrscr();
 printf("Enter a string:");
 gets(str);
 printf("Enter a no of chars from right");
 scanf("%d",&no);
 int len = strlen(str);
  for(i=(len-no);i< len;i++)
    /* printf("%c",str[i]);*/
   newarr[j++]=str[i];
   newarr[j]='\0';
  printf("%s",newarr);
getch();
}
